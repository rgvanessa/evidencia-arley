let ocancha= [{
    ocupacion: true, nombre: "bernaveu"
}]

let mensaje = cancha[0].ocupacion ? `la cancha llamada ${cancha[0].nombre} esta ocupada` : 
`la cancha llamada ${cancha[0].nombre}esta disponible`;
console.log(mensaje)
